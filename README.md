# Two Weeks Team Homebrew Tap

Install Memex with Homebrew:

```sh
brew install --cask two-weeks-team/tap/memex
```

Upgrade later with:

```sh
brew upgrade --cask memex
```

Memex currently ships an Apple Silicon macOS DMG.
